# ──────────────────────────────────────────────────────────────────────────────
# Examora Backend — Render.com deployment (backend service only)
#
# Note: This file deploys the Express backend service on Render only.
# The Next.js frontend is deployed separately on Vercel.
#
# Steps:
#   1. Create a free PostgreSQL + Redis (or Upstash) instance on Render.
#   2. In the dashboard, create a new "Web Service" pointing at this repo:
#        Root Directory: (empty — monorepo root)
#        Build Command:  npm ci && npx prisma generate && npx prisma migrate deploy && npm run server:build
#        Start Command:  npm run server:start
#      (or use "Blueprint" and reference this file directly)
#   3. Provide the env vars below (DATABASE_URL, REDIS_URL, JWT_SECRET, ...).
# ──────────────────────────────────────────────────────────────────────────────

services:
  - type: web
    name: examora-backend
    env: node
    region: oregon
    plan: free
    rootDir: .
    buildCommand: npm ci && npx prisma generate && npx prisma migrate deploy && npm run server:build
    startCommand: npm run server:start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: DATABASE_URL
        sync: false            # set manually — Render PostgreSQL connection string
      - key: REDIS_URL
        sync: false            # optional — set manually if using Redis for Socket.io
      - key: JWT_SECRET
        generateValue: true
      - key: SESSION_SECRET
        generateValue: true
      - key: AUTH_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://examora.vercel.app     # ← replace with your Vercel frontend URL
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        sync: false            # Gmail address for Nodemailer invites
      - key: SMTP_PASS
        sync: false            # Gmail app password
      - key: SMTP_FROM
        sync: false            # e.g. "Examora" <noreply@example.com>
      - key: GROQ_API_KEY
        sync: false            # optional — enables free-tier AI question generation
